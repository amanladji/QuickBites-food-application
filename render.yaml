services:
  - type: web
    name: quickbite
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/quickbite.jar
    envVars:
      - key: PORT
        value: 8080
      - key: MONGO_URI
        sync: false
    healthCheckPath: /index.html
